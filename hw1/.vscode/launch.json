{
    "version": "0.2.0",
    "configurations": [
        

        {
            "name": "Build LaTeX Document",
            "type": "extensionHost",
            "request": "launch",
            "preLaunchTask": "Compile LaTeX with XeLaTeX",
            "postDebugTask": "Open PDF"
        }
    ]
}
